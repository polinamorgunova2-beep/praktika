namespace FinanceTracker.Models
{
    // Роли пользователей приложения.
    // Администратор — полный доступ; Гость — только просмотр.
    public enum UserRole
    {
        Admin,
        Guest
    }
}
